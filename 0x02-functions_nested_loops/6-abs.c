#inlcude "main.h"
/**
 *  _abs - function that computes the absolute value of an integer.
 *  @ab: input letter
 *  Return: Always success
 */
int _abs(int ab)
{
return (ab * ((ab > )) - (ab < 0))
	;
}
